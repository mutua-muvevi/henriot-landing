import { Box, Typography } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const ForexReviews = () => {
	return (
		<StyledWrapper>
			<Typography variant="h1">ForexReviews</Typography>
		</StyledWrapper>
	)
}

export default ForexReviews