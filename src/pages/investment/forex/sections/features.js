import { Box, Typography } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const ForexFeatures = () => {
	return (
		<StyledWrapper>
			<Typography variant="h1">ForexFeatures</Typography>
		</StyledWrapper>
	)
}

export default ForexFeatures