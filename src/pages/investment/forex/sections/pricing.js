import { Box, Typography } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const ForexPricing = () => {
	return (
		<StyledWrapper>
			<Typography variant="h1">ForexPricing</Typography>
		</StyledWrapper>
	)
}

export default ForexPricing