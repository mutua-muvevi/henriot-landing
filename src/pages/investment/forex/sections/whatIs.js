import { Box, Typography } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const ForexWhatIs = () => {
	return (
		<StyledWrapper>
			<Typography variant="h1">ForexWhatIs</Typography>
		</StyledWrapper>
	)
}

export default ForexWhatIs