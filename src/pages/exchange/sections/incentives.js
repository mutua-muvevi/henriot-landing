import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledExchangeIncentives = styled(Box)(({ theme }) => ({
	
}))

const ExchangeIncentives = () => {
	return (
		<StyledExchangeIncentives>
			<div>ExchangeIncentives</div>
		</StyledExchangeIncentives>
	)
}

export default ExchangeIncentives