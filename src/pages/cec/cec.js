import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledCEC = styled(Box)(({ theme }) => ({
	
}))

const CEC = () => {
	return (
		<StyledCEC>
			<div>CEC</div>
		</StyledCEC>
	)
}

export default CEC