import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledNews = styled(Box)(({ theme }) => ({
	
}))

const News = () => {
	return (
		<StyledNews>
			<div>News</div>
		</StyledNews>
	)
}

export default News