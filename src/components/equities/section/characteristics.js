import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledCharacteristics = styled(Box)(({ theme }) => ({

}))

const Characteristics = () => {
	return (
		<StyledCharacteristics>
			Characteristics
		</StyledCharacteristics>
	)
}

export default Characteristics