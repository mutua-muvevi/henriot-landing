import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledFocus = styled(Box)(({ theme }) => ({

}))

const Focus = () => {
	return (
		<StyledFocus>
			Focus
		</StyledFocus>
	)
}

export default Focus